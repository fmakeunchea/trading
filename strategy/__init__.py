"""Trading bot foundational package — Phase 1.

This package contains only the foundational layer: typed contracts, config,
atomic state, append-only audit log, broker wrapper, reconciliation, and
the risk engine. Signal logic and orchestrator live in later phases.
"""

__version__ = "0.1.0"
